name: Frontend CI
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: 18.8
    steps:
      - name: Checkout Repository
          uses: actions/checkout@v4
      - run: npm install
